<h3>Show Post</h3>

<div class "row">
	<div class="col-md-8 well">
	  <h3><%= @post.title%></h3>
	  <p>
	    <%= @post.content%>
	  </p>
	</div>
</div>

<% @post.comments.each do |comment|%>
	<div class="col-md-6 well">
	  <p>
	    <%= comment.content%>
	  </p>
	</div>
<% end %>

<br>
<br>
<%= link_to 'go back to all posts', posts_path %>
<!-- 第二版
<h3>All Posts</h3>

<% @posts.each do |post| %>
  <div class="row">
    <div class="well col-md-6">
      <p>
        <h4><%= post.title %></h4>
      </p>
      <p>
        <%= post.content %>
      </p>
      <br>
      <span>
        <%= link_to "show this post", post_path(post)%>
      </span>
    </div>
  </div>
<% end %>

以上為加了Bootstrap後的用法
以下第一版
<h1><%= @post.title %></h1>
<hr>
<p>
<%= @post.content %>
</p>

<!-- 連結回到 index page, posts_path 代表 Post 的 index 頁面 --
<%= link_to 'go back to all posts', posts_path %>
-->